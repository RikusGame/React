import React from "react";

const Navbar = () => (
  <header style={{
    background: "#1e3c72", color: "#fff", padding: "1rem", textAlign: "center"
  }}>
    <h1>Panel Admin</h1>
  </header>
);

export default Navbar;